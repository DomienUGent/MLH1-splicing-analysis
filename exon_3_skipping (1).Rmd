---
title: "R Notebook"
output: html_notebook
---

This is an [R Markdown](http://rmarkdown.rstudio.com) Notebook. When you execute code within the notebook, the results appear beneath the code. 

Try executing this chunk by clicking the *Run* button within the chunk or by placing your cursor inside it and pressing *Ctrl+Shift+Enter*. 

```{r}
exon3skip_filteredtissues <- merged_exonskipping_long %>% dplyr::filter(donor_exon == 2, acceptor_exon == 4)
intron2_3_filteredtissues <- merged_normalsplicing_long %>% dplyr::filter(donor_exon == 3 | acceptor_exon == 3)
```

```{r}
combined_filteredtissues_exon3 <- bind_rows(intron2_3_filteredtissues, exon3skip_filteredtissues)
combined_filteredtissues_ordered_exon3 <- combined_filteredtissues_exon3 %>%
  mutate(order_rank = case_when(
    end.x == 37000954 ~ 1,
    start.x == 37001054 ~ 2,
    skipped_exons == 1 ~ 3,
    TRUE ~ 4
  )) %>%
  arrange(sample, order_rank) %>%
  dplyr::select(-order_rank)
# order the events : per rail_id, first row is intron 11 split read count, second row is intron 12 split read count, third row is exon 12 split read count

combined_filteredtissues_ordered_exon3 <- combined_filteredtissues_ordered_exon3 %>% dplyr::select(start.x, end.x, junction_id.x, donor_exon, acceptor_exon, event_class, gtex.subjid, read_counts, gtex.smtsd, gtex.sex, gtex.age, recount_qc.bc_auc.all_reads_annotated_bases, sample, `recount_qc.star.uniquely_mapped_reads_._both`, recount_qc.junction_avg_coverage, gtex.smrin, gtex.smnabtch, gtex.smgebtch, gtex.dthhrdy)
# select only the required columns to make the dataframe simpler
```
```{r}
any(is.na(combined_filteredtissues_ordered_exon3))
#check for any NA's -> must return FALSE
na_cols_exon3 <- combined_filteredtissues_ordered_exon3 %>%
  dplyr::select(where(~ any(is.na(.))))
na_rows_exon3 <- combined_filteredtissues_ordered_exon3 %>%
  filter(if_any(everything(), is.na))
# only Na's in gtex.dthhrry
```

```{r}
combined_filteredtissues_ordered_exon3 <- combined_filteredtissues_ordered_exon3 %>%
  mutate(junction_type = case_when(
    junction_id.x == 3699671037000954 ~ "2_3",
    junction_id.x == 3700105437004400 ~ "3_4",
    junction_id.x == 3699671037004400 ~ "2_4",
    TRUE ~ NA_character_
  )) %>%
  dplyr::select(-donor_exon, -acceptor_exon)
```

```{r}
combined_filteredtissues_ordered_exon3 <- combined_filteredtissues_ordered_exon3 %>%
  mutate(order_rank = case_when(
    end.x == 37000954 ~ 1,
    start.x == 37001054 ~ 2,
    event_class == "exon skipping" ~ 3,
    TRUE ~ 4
  )) %>%
  arrange(sample, order_rank) %>%
  dplyr::select(-order_rank)
```

```{r}
onerow_per_sample_exon3 <- combined_filteredtissues_ordered_exon3 %>%
  dplyr::group_by(sample) %>%
  dplyr::summarise(
    inc_2_3 = read_counts[junction_type == "2_3"],
    inc_3_4 = read_counts[junction_type == "3_4"],
    total_inclusion = sum(
      read_counts[junction_type == "2_3"] +
      read_counts[junction_type == "3_4"],
      na.rm = TRUE),
    skipping = read_counts[junction_type == "2_4"],
    SEX = gtex.sex[!is.na(gtex.sex)][1],
    AGE = gtex.age[!is.na(gtex.age)][1],
    SUBJID = gtex.subjid[!is.na(gtex.subjid)][1],
    SMTSD = gtex.smtsd[!is.na(gtex.smtsd)][1],
    bc_auc.all_reads_annotated_bases =
      recount_qc.bc_auc.all_reads_annotated_bases[!is.na(recount_qc.bc_auc.all_reads_annotated_bases)][1],
    `star.uniquely_mapped_reads_%_both` = `recount_qc.star.uniquely_mapped_reads_._both`[!is.na(`recount_qc.star.uniquely_mapped_reads_._both`)][1],
    junction_avg_coverage = recount_qc.junction_avg_coverage[!is.na(recount_qc.junction_avg_coverage)][1],
    RIN_number = gtex.smrin[!is.na(gtex.smrin)][1],
    Nucleic_acid_isolation_batch = gtex.smnabtch[!is.na(gtex.smnabtch)][1],
    Genotype_or_expression_batch = gtex.smgebtch[!is.na(gtex.smgebtch)][1],
    Classification_death = gtex.dthhrdy[!is.na(gtex.dthhrdy)][1],
    
    .groups = "drop"
  )
```

```{r}
replicates_exon3 <- onerow_per_sample_exon3 %>%
  group_by(SUBJID, SMTSD) %>%
  filter(n() >= 2) %>%
  ungroup()
```

```{r}
merged_onerow_per_sample_exon3 <- onerow_per_sample_exon3 %>%
  left_join(
    mlh1_alltissues_cpm %>%
      dplyr::select(sample_id, mlh1_cpm, raw_counts),
    by = c("sample" = "sample_id")
  )

```

```{r}
merged_onerow_per_sample_exon3$AGE <- as.factor(merged_onerow_per_sample_exon3$AGE)
merged_onerow_per_sample_exon3$SEX <- as.factor(merged_onerow_per_sample_exon3$SEX)
merged_onerow_per_sample_exon3$Classification_death <- as.factor(merged_onerow_per_sample_exon3$Classification_death)
```

```{r}
quantile(merged_onerow_per_sample_exon3$total_inclusion, probs = c(0.1, 0.25, 0.5, 0.75))
```
```{r}
merged_onerow_per_sample_exon3_filtered <- merged_onerow_per_sample_exon3 %>%
  filter(total_inclusion > 11)
```

```{r}
table(merged_onerow_per_sample_exon3$SMTSD)
# look how many samples per tissue are left over after filtering

table(merged_onerow_per_sample_exon3_filtered$SMTSD)
# look how many samples per tissue are left over after filtering
```

```{r}
saveRDS(merged_onerow_per_sample_exon3_filtered, file = "merged_onerow_per_sample_exon3_filtered.rds")
saveRDS(merged_onerow_per_sample_exon3, file = "merged_onerow_per_sample_exon3.rds")
```

```{r}
# unfiltered model without all the covariats (simplest model)

simplest_model_exon3 <- glmmTMB(
  cbind(skipping, total_inclusion) ~ 
    SMTSD + 
    SEX + 
    AGE +
    (1 | SUBJID),
  
  family = betabinomial(),
  data = merged_onerow_per_sample_exon3
)

emm_tmb_simplest_model_exon3 <- emmeans(simplest_model_exon3, ~ SMTSD)
pairs_tmb_simplest_model_exon3 <- pairs(emm_tmb_simplest_model_exon3, adjust = "BH")
simplest_model_exon3
pairs_tmb_simplest_model_exon3
```

```{r}
emm_thisplot_simplest_model_exon3 <- emmeans(simplest_model_exon3, ~ SMTSD, type = "response")
emm_thisplot_simplest_model_exon3_df <- as.data.frame(emm_thisplot_simplest_model_exon3)
breast_prob_simplest_model_exon3 <- emm_thisplot_simplest_model_exon3_df$prob[emm_thisplot_simplest_model_exon3_df$SMTSD == "Breast - Mammary Tissue"]

emm_thisplot_simplest_model_exon3_df <- emm_thisplot_simplest_model_exon3_df %>%
  mutate(
    fold_vs_breast = breast_prob_simplest_model_exon3 / prob
  )
```


```{r}
# simplest model + RIN number -> now main model
mainmodel_exon3 <- glmmTMB(
  cbind(skipping, total_inclusion) ~ 
    SMTSD + 
    SEX + 
    AGE +
    RIN_number +
    (1 | SUBJID),
  
  family = betabinomial(),
  data = merged_onerow_per_sample_exon3
)

emm_tmb_mainmodel_exon3 <- emmeans(mainmodel_exon3, ~ SMTSD)
pairs_tmb_mainmodel_exon3 <- pairs(emm_tmb_mainmodel_exon3, adjust = "BH")
mainmodel_exon3
pairs_tmb_mainmodel_exon3
```
```{r}
emm_exon3 <- emmeans(mainmodel_exon3, ~ SMTSD)
levels(mainmodel_exon3$data$SMTSD)
contr_exon3 <- contrast(emm_exon3, method = "trt.vs.ctrl", ref = "Breast - Mammary Tissue")
summary(contr_exon3, adjust = "BH")
```

```{r}
emm_thisplot_mainmodel_exon3 <- emmeans(mainmodel_exon3, ~ SMTSD, type = "response")
emm_thisplot_mainmodel_exon3_df <- as.data.frame(emm_thisplot_mainmodel_exon3)
breast_prob_mainmodel_exon3 <- emm_thisplot_mainmodel_exon3_df$prob[emm_thisplot_mainmodel_exon3_df$SMTSD == "Breast - Mammary Tissue"]

emm_thisplot_mainmodel_exon3_df <- emm_thisplot_mainmodel_exon3_df %>%
  mutate(
    fold_vs_breast = breast_prob_mainmodel_exon3 / prob
  )
```


```{r}
# unfiltered model with the technical and expression covariats
model_tmb_exon3_covariats_recount3 <- glmmTMB(
  cbind(skipping, total_inclusion) ~ 
    SMTSD + 
    SEX + 
    AGE +
    scale(bc_auc.all_reads_annotated_bases) +
    scale(`star.uniquely_mapped_reads_%_both`) +
    scale(junction_avg_coverage) +
    scale(mlh1_cpm) + 
    (1 | SUBJID),
  
  family = betabinomial(),
  data = merged_onerow_per_sample_exon3
)

emm_tmb_exon3_covariats_recount3 <- emmeans(model_tmb_exon3_covariats_recount3, ~ SMTSD)
pairs_tmb_exon3_covariats_recount3 <- pairs(emm_tmb_exon3_covariats_recount3, adjust = "BH")
model_tmb_exon3_covariats_recount3
pairs_tmb_exon3_covariats_recount3
```

```{r}
emm_thisplot_exon3_covariats_recount3 <- emmeans(model_tmb_exon3_covariats_recount3, ~ SMTSD, type = "response")
emm_thisplot_exon3_covariats_df_recount3 <- as.data.frame(emm_thisplot_exon3_covariats_recount3)
breast_prob_exon3_covariats_recount3 <- emm_thisplot_exon3_covariats_df_recount3$prob[emm_thisplot_exon3_covariats_df_recount3$SMTSD == "Breast - Mammary Tissue"]

emm_thisplot_exon3_covariats_df_recount3 <- emm_thisplot_exon3_covariats_df_recount3 %>%
  mutate(
    fold_vs_breast = breast_prob_exon3_covariats_recount3 / prob
  )
```

```{r}
# compare models
emm_thisplot_simplest_model_exon3_df
emm_thisplot_mainmodel_exon3_df
emm_thisplot_exon3_covariats_df_recount3
```

```{r}
summary(simplest_model_exon3)
summary(mainmodel_exon3)
summary(model_tmb_exon3_covariats_recount3)
```

```{r}
exon3_dist_exact_base_recount3 <- merged_onerow_per_sample_exon3 %>%
  mutate(
    skipping_capped = ifelse(skipping >= 10, "10+", as.character(skipping))
  ) %>%
  group_by(SMTSD, skipping_capped) %>%
  summarise(n = n(), .groups = "drop") %>%
  group_by(SMTSD) %>%
  mutate(
    total_tissue = sum(n),
    pct = 100 * n / total_tissue
  )

ggplot(
  exon3_dist_exact_base_recount3,
  aes(
    x = factor(skipping_capped,
               levels = c(as.character(0:9), "10+")),
    y = pct
  )
) +
  geom_col(fill = "steelblue", width = 1.0) +
  geom_text(
    aes(label = sprintf("%.1f%%", pct)),
    vjust = -0.3,
    size = 1.5
  ) +
  facet_wrap(~ SMTSD) +
  theme_classic() +
  labs(
    x = "Exon 3 skipping read count",
    y = "Percentage of samples within tissue"
  ) +
  ylim(0, max(exon3_dist_exact_base_recount3$pct) * 1.1)
```

```{r}
exon3_dist_exact_base_recount3 <- merged_onerow_per_sample_exon3 %>%
  mutate(
    psi_sum = skipping / (skipping + total_inclusion),
    psi_bin = cut(
      psi_sum,
      breaks = c(
        0,
        0.001,
        0.005,
        0.01,
        0.02,
        0.05,
        0.1,
        0.2,
        1
      ),
      include.lowest = TRUE
    )
  ) %>%
  group_by(SMTSD, psi_bin) %>%
  summarise(n = n(), .groups = "drop") %>%
  group_by(SMTSD) %>%
  mutate(pct = 100 * n / sum(n))

ggplot(exon3_dist_exact_base_recount3, aes(x = psi_bin, y = pct)) +
  geom_col(fill = "steelblue", width = 1) +
  geom_text(
    aes(label = sprintf("%.1f%%", pct)),
    vjust = -0.3,
    size = 1.8
  ) +
  facet_wrap(~ SMTSD) +
  theme_classic() +
  theme(axis.text.x = element_text(size = 5, angle = 45, hjust = 1)) +
  labs(
    x = "PSI bin",
    y = "Percentage of samples within tissue"
  ) +
  ylim(0, max(exon3_dist_exact_base_recount3$pct) * 1.1)
```

```{r}
pairs_tmb_mainmodel_exon3_df <- as.data.frame(pairs_tmb_mainmodel_exon3)

# Filter Breast comparisons
pairs_tmb_mainmodel_exon3_df <- pairs_tmb_mainmodel_exon3_df %>%
  filter(grepl("Breast - Mammary Tissue", contrast))

# Compute effects + significance + modified labels
pairs_tmb_mainmodel_exon3_df <- pairs_tmb_mainmodel_exon3_df %>%
  mutate(
    odds_ratio = exp(estimate),
    percent_difference = (odds_ratio - 1) * 100,
    lower_ci = (exp(estimate - 1.96 * SE) - 1) * 100,
    upper_ci = (exp(estimate + 1.96 * SE) - 1) * 100,

    sig = case_when(
      p.value < 0.001 ~ "***",
      p.value < 0.01  ~ "**",
      p.value < 0.05  ~ "*",
      TRUE ~ ""
    ),

    # clean tissue label + add stars
    tissue = gsub(".*Breast - Mammary Tissue - ", "", contrast),
    tissue = paste0(tissue, sig)
  )

# Plot with stars in labels
ggplot(pairs_tmb_mainmodel_exon3_df,
       aes(x = percent_difference, y = reorder(tissue, percent_difference))) +
  geom_point(size = 3) +
  geom_errorbarh(
    aes(xmin = lower_ci, xmax = upper_ci),
    height = 0.2
  ) +
  geom_vline(xintercept = 0, linetype = "dashed") +
  theme_minimal() +
  labs(
    x = "Percent difference in exon skipping odds vs Breast",
    y = "Tissue"
  )
```

```{r}
# this plot needs to be made with inc_11_12, not total_inclusion for a better psi
emm_resp_mainmodel_exon3 <- summary(emm_tmb_mainmodel_exon3, type = "response")
emm_resp_mainmodel_exon3_df <- as.data.frame(emm_resp_mainmodel_exon3)
ggplot(emm_resp_mainmodel_exon3_df, aes(x = reorder(SMTSD, prob), y = prob)) +
  geom_point(size = 3) +
  geom_errorbar(aes(ymin = asymp.LCL, ymax = asymp.UCL), width = 0.2) +
  coord_flip() +
  labs(
    x = "Tissue",
    y = "Predicted exon skipping proportion",
    title = "MLH1 exon skipping across tissues"
  ) +
  theme_minimal()
```

```{r}
model_tmb_exon3_intron3_mainmodel <- glmmTMB(
  cbind(skipping, inc_3_4) ~ 
    SMTSD + 
    SEX + 
    AGE +
    RIN_number +
    (1 | SUBJID),
  
  family = betabinomial(),
  data = merged_onerow_per_sample_exon3
)

emm_tmb_exon3_intron3_mainmodel <- emmeans(model_tmb_exon3_intron3_mainmodel, ~ SMTSD)
pairs_tmb_exon3_intron3_mainmodel <- pairs(emm_tmb_exon3_intron3_mainmodel, adjust = "BH")
model_tmb_exon3_intron3_mainmodel
pairs_tmb_exon3_intron3_mainmodel
```

```{r}
emm_thisplot_exon3_intron3_mainmodel <- emmeans(model_tmb_exon3_intron3_mainmodel, ~ SMTSD, type = "response")
emm_thisplot_exon3_intron3_mainmodel_df <- as.data.frame(emm_thisplot_exon3_intron3_mainmodel)
breast_prob_exon3_intron3_mainmodel <- emm_thisplot_exon3_intron3_mainmodel_df$prob[emm_thisplot_exon3_intron3_mainmodel_df$SMTSD == "Breast - Mammary Tissue"]

emm_thisplot_exon3_intron3_mainmodel_df <- emm_thisplot_exon3_intron3_mainmodel_df %>%
  mutate(
    fold_vs_breast = breast_prob_exon3_intron3_mainmodel / prob
  )
emm_thisplot_exon3_intron3_mainmodel_df
```

```{r}
emm_resp_exon3_intron3_mainmodel <- summary(emm_tmb_exon3_intron3_mainmodel, type = "response")
emm_response_exon3_intron3_mainmodel_df <- as.data.frame(emm_resp_exon3_intron3_mainmodel)
ggplot(emm_response_exon3_intron3_mainmodel_df, aes(x = reorder(SMTSD, prob), y = prob)) +
  geom_point(size = 3) +
  geom_errorbar(aes(ymin = asymp.LCL, ymax = asymp.UCL), width = 0.2) +
  coord_flip() +
  labs(
    x = "Tissue",
    y = "Predicted exon skipping proportion",
    title = "MLH1 exon skipping across tissues"
  ) +
  theme_minimal()
```

```{r}
library(DHARMa)

sim_res_exon3 <- simulateResiduals(mainmodel_exon3)
plot(sim_res_exon3)
```

```{r}
testOutliers(sim_res_exon3, type = "bootstrap")
```

```{r}
plotResiduals(sim_res_exon3, merged_onerow_per_sample_exon3$SMTSD)
plotResiduals(sim_res_exon3, merged_onerow_per_sample_exon3$RIN_number)
```

```{r}
testDispersion(sim_res_exon3)
```

```{r}
testZeroInflation(sim_res_exon3)
```



